{
  "name": "@visuelconcept/wui-para",
  "version": "0.1.0",
  "mode": "source",
  "description": "PARA — datapoint parametrization page (source distribution: built on the target's WebUI Runtime workspace so the bundle matches its version).",
  "requires": ["a WebUI Runtime workspace (@wincc-oa/webui-runtime)", "@visuelconcept/wui-webserver (for the /api/para backend)"],
  "frontend": {
    "standalonePagesDir": "frontend/standalone-pages",
    "menuFragment": "frontend/menu.fragment.jsonc"
  },
  "backend": {
    "module": "backend/modules/para"
  }
}
