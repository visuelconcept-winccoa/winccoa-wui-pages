{
  "name": "rtsp-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "RTSP camera streaming proxy (WinCC OA JavaScript Manager). Relays RTSP video streams to the browser over WebSocket (MPEG1-TS for JSMpeg) via ffmpeg, sharing one RTSP connection across all connected web clients.",
  "main": "index.js",
  "dependencies": {
    "express": "^4.21.2",
    "express-ws": "^5.0.2",
    "ffmpeg-static": "^5.2.0",
    "rtsp-relay": "^1.0.6"
  }
}
